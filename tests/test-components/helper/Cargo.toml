[package]
name = "helper"
version = "0.1.0"
edition = "2024"

[dependencies]
wit-bindgen = { version = "0.61.1", optional = true }

[features]
default = ["define-component"]
define-component = ["dep:wit-bindgen"]
